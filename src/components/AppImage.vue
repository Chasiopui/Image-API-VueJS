<template>
<div></div>
</template>

<script>
// import api from '../services/api'

export default {
  props: ['element', 'rowIndex'],
  data() {
    return {
      // searchKey: '',
      // collections: [], 
      // autoClose: this.element.needAutoClose === false ? false : true
    }
  },
  methods: {
  }
}
</script>